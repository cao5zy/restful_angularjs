import { EmailExtenderDirective } from './email-extender.directive';

describe('EmailExtenderDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailExtenderDirective();
    expect(directive).toBeTruthy();
  });
});
