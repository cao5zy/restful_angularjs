import { UniqueUserNameDirective } from './unique-user-name.directive';

describe('UniqueUserNameDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueUserNameDirective();
    expect(directive).toBeTruthy();
  });
});
